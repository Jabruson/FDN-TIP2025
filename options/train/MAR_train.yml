name: MAR
model_type: ImageRestorationModel_ipretrain
scale: 1
num_gpu: 4
manual_seed: 42
use_ratio: true

datasets:
  train:
    name: lolblur-train
    type: PairedImageDataset
    dataroot_gt: /data/tuluwei/dataset/lolblur/train/high_sharp_scaled/*/*
    dataroot_lq: /data/tuluwei/dataset/lolblur/train/low_blur_noise/*/*

    filename_tmpl: '{}'
    io_backend:
      type: lmdb

    gt_size: 256
    use_flip: true
    use_rot: true

    # data loader
    use_shuffle: true
    num_worker_per_gpu: 8
    batch_size_per_gpu: 4
    dataset_enlarge_ratio: 1
    prefetch_mode: ~

  val:
    name: lolblur-test
    type: PairedImageDataset
    dataroot_gt: /data/tuluwei/dataset/lolblur/test/high_sharp_scaled/*/*
    dataroot_lq: /data/tuluwei/dataset/lolblur/test/low_blur_noise/*/*
    io_backend:
      type: lmdb

  val_tiny:
    name: lolblur-test
    type: PairedImageDataset
    dataroot_gt: /datatuluwei/dataset/lolblur_test_tinya2/high_sharp_scaled/*/*
    dataroot_lq: /data/tuluwei/dataset/lolblur_test_tinya2/low_blur_noise/*/*
    io_backend:
      type: lmdb


network_g:
  type: MAR


# path
path:
  pretrain_network_g:
  strict_load_g:
  resume_state:

# training settings
train:
  optim_g:
    type: AdamW
    lr: !!float 1e-4
    weight_decay: !!float 1e-4
    betas: [0.9, 0.9]

  scheduler:
    type: TrueCosineAnnealingLR
    T_max: 100000
    eta_min: !!float 1e-7

  total_iter: 100000
  warmup_iter: -1 # no warm up

  # losses
  pixel_opt:
    type: L1Loss
    loss_weight: 1.0
    reduction: mean

  fft_loss_opt:
    type: FFTLoss
    loss_weight: 0.1
    reduction: mean

# validation settings
val:
  val_freq: !!float 1e2
  save_img: true


  metrics:
    psnr: # metric name, can be arbitrary
      type: calculate_psnr
      crop_border: 0
      test_y_channel: false
    ssim:
      type: calculate_ssim
      crop_border: 0  
      test_y_channel: false

# logging settings
logger:
  print_freq: 100
  save_checkpoint_freq: !!float 1e2
  use_tb_logger: true
  wandb:
    project: ~
    resume_id: ~

# dist training settings
dist_params:
  backend: nccl
  port: 29500
